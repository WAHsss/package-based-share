import type { Meta } from '@storybook/react';
import { IsOdd } from './is-odd';

const Story: Meta<typeof IsOdd> = {
  component: IsOdd,
  title: 'IsOdd',
};
export default Story;

export const Primary = {
  args: {},
};
